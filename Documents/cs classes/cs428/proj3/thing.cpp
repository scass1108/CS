#include <vector>
#include <iostream>
#include <algorithm>
#include <sstream>
#include <math.h>

using namespace std;

struct node{

	int self_number;
	vector<int> routing_table;
	
}

int main(int argc, char *argv[]) {

//open file, read contents to appropriate variables
//set up routing table for specific node
//set up udp connection
//set up routing and hello
//make packet prototype
//use distance vector, implement as linked list?/vector?
//is multi-threaded so set that up and make mutex lock on table
//send hello packets to make sure connections are still active
//update routing table





}
